import React, { useState } from 'react';
import Button from './Button'
import './App.css';

const App: React.FC = () => {

  return (
    <div className="App">
      <Button
        message = "yo"
      />
    </div>
  );
}

export default App;
